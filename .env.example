# Optional overlay or container-style configuration.
# A TOML file (config.example.toml) is the primary config.
XMPP_UTILS_JID=xmpp-utilities@telepath.im
XMPP_UTILS_PASSWORD=hunter2
XMPP_UTILS_NICK="XMPP Utilities"
XMPP_UTILS_MUCS=offtopic@room.telepath.im,bot-testing@room.telepath.im
# XMPP_UTILS_INVITE_ENABLED=false
# XMPP_UTILS_INVITE_PERSIST=true
# XMPP_UTILS_INVITE_MAX_ROOMS=32
# XMPP_UTILS_INVITE_ALLOW_FROM=
# XMPP_UTILS_INVITE_ALLOW_DOMAINS=
# XMPP_UTILS_INVITE_DENY_FROM=
# XMPP_UTILS_INVITE_ALLOW_MUC_HOSTS=
